<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace = "smelldetector.mapper.FeatureEnvyMapper">
	<insert id="insertFeatureEnvy" parameterType="smelldetector.pojo.FeatureEnvy">
		insert
		into featureenvy
		(projectName, className, methodName, atfd, possibility)
		values
		(#{projectName}, #{className}, #{methodName}, #{atfd}, #{possibility})
	</insert>
</mapper>